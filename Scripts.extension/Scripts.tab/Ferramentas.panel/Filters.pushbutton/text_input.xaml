<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar texto" Width="520" Height="150"
        MinWidth="420" MaxWidth="760"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI" FontSize="12"
        PreviewKeyDown="text_input_preview_key_down">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblPrompt" Grid.Row="0" Margin="0,0,0,8"
                   Text="Texto" FontWeight="SemiBold"/>

        <TextBox x:Name="txtValue" Grid.Row="1" Height="30"
                 VerticalContentAlignment="Center"
                 FontFamily="Segoe UI" FontSize="14"
                 Padding="8,0"/>

        <Grid Grid.Row="2"/>

        <DockPanel Grid.Row="3" LastChildFill="False">
            <Button DockPanel.Dock="Right" Content="OK" Width="84" Height="28"
                    FontWeight="SemiBold" Click="ok_click"/>
            <Button DockPanel.Dock="Right" Content="Cancelar" Width="84" Height="28"
                    Margin="0,0,8,0" Click="cancel_click"/>
        </DockPanel>
    </Grid>
</Window>
